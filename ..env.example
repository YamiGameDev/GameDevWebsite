# YouTube Data API v3 Key
# Get your free API key from: https://console.developers.google.com/
# 1. Create a new project or select existing one
# 2. Enable YouTube Data API v3
# 3. Create credentials (API Key)
# 4. Copy the API key below
REACT_APP_YOUTUBE_API_KEY=your_youtube_api_key_here

# Optional: Set quotas and restrictions for the API key
# Recommended: Restrict to your domain and YouTube Data API v3 only